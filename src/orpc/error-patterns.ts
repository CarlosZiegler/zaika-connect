export const FILE_TOO_LARGE_PATTERN = /(file size exceeds|payload too large|max(?:imum)? allowed size|too large)/iu;
export const INVALID_FILE_FORMAT_PATTERN = /(invalid file format|unsupported media type|file type .* is not allowed|mime type)/iu;
